export const TitleFormNavigationArrow = ({title}) => {
    return (
        <div className="w-65 flex justify-center">
            <p className="text-lg font-bold">{title.toUpperCase()}</p>
        </div>
    )
}